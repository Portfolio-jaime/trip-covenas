// Filled in after `terraform apply` — copy the `api_summary_endpoint` output here.
// This value is a public endpoint URL, not a secret; safe to commit.
window.DASHBOARD_API_URL = "https://rbzg7ddzyh.execute-api.us-east-1.amazonaws.com/api/summary";
